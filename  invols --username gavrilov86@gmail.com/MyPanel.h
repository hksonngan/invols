#ifndef MYPANEL_INC
#define MYPANEL_INC

#include "AllInc.h"
#include "wxIncludes.h"
/*
class MyPanel: public wxPanel
{
public:
    MyPanel(wxWindow *frame);

	
	void OnSliderZ(wxCommandEvent& WXUNUSED(event));
	void OnSliderBox(wxCommandEvent& WXUNUSED(event));
	
	void OnListStereo(wxCommandEvent& WXUNUSED(event));

	void OnCheckBB(wxCommandEvent& event);
	void OnBtnColor1(wxCommandEvent& event);
	void OnBtnColor2(wxCommandEvent& event);
	void OnBtnDir(wxCommandEvent& event);
	void Update_(bool self);

	static MyPanel*inst;

private:
	wxSlider *m_slider_z[20], *m_slider_eye,*m_slider_my_z;

	wxChoice *m_box;
	wxButton *btn_c1, *btn_c2,*btn_dir;
	wxCheckBox *m_check_box_BB,*m_check_box_autofps,*m_check_use_accel_struct;
	wxStaticText *static_dir;

	 DECLARE_EVENT_TABLE()
};
*/

#endif